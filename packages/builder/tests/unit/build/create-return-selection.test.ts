import { createReturnSelection } from "../../../src/build";

describe("createReturnSelection", () => {
  test("should be function", () => {
    expect(createReturnSelection).toBeInstanceOf(Function);
  });
});
