import { createSetSelectionAndParams } from "../../../src/build";

describe("createSetSelectionAndParams", () => {
  test("should be function", () => {
    expect(createSetSelectionAndParams).toBeInstanceOf(Function);
  });
});
