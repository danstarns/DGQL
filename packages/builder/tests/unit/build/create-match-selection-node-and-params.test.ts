import { createMatchSelectionNodeAndParams } from "../../../src/build";

describe("createMatchSelectionNodeAndParams", () => {
  test("should be function", () => {
    expect(createMatchSelectionNodeAndParams).toBeInstanceOf(Function);
  });
});
