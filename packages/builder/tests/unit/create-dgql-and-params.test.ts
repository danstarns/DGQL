import createDGQLAndParams from "../../src/create-dgql-and-params";

describe("createDGQLAndParams", () => {
    test("should be function", () => {
        expect(createDGQLAndParams).toBeInstanceOf(Function);
    });
});
