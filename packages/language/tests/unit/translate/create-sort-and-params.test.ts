import { createSortAndParams } from "../../../src/translate";

describe("createSortAndParams", () => {
    test("should be a function", () => {
        expect(createSortAndParams).toBeInstanceOf(Function);
    });
});
