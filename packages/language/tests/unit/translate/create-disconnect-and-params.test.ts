import { createDisconnectAndParams } from "../../../src/translate";

describe("createDisconnectAndParams", () => {
  test("should be a function", () => {
    expect(createDisconnectAndParams).toBeInstanceOf(Function);
  });
});
