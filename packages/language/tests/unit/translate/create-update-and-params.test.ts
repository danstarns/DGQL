import { createUpdateAndParams } from "../../../src/translate";

describe("createUpdateAndParams", () => {
  test("should be a function", () => {
    expect(createUpdateAndParams).toBeInstanceOf(Function);
  });
});
