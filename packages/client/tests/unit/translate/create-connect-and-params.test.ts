import { createConnectAndParams } from "../../../src/translate";

describe("createConnectAndParams", () => {
  test("should be a function", () => {
    expect(createConnectAndParams).toBeInstanceOf(Function);
  });
});
