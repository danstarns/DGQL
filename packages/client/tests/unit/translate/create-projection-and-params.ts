import { createProjectionAndParams } from "../../../src/translate";

describe("createProjectionAndParams", () => {
  test("should be a function", () => {
    expect(createProjectionAndParams).toBeInstanceOf(Function);
  });
});
