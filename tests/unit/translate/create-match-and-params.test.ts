import { createMatchAndParams } from "../../../src/translate";

describe("createMatchAndParams", () => {
    test("should be a function", () => {
        expect(createMatchAndParams).toBeInstanceOf(Function);
    });
});
